# Firebase Rules 적용 가이드

## 적용 방법

1. Firebase 콘솔(https://console.firebase.google.com) 접속
2. **factpepe-1bb4f** 프로젝트 → **Realtime Database** → **규칙** 탭
3. 현재 룰을 백업(복사해서 별도 저장)
4. `firebase.rules.json` 내용을 복사 → 붙여넣기 → **게시**

> 백업은 반드시. 룰 실수로 앱이 즉시 죽을 수 있습니다.

## 적용 전 시뮬레이션 (필수)

콘솔 우상단 **"시뮬레이터"** 클릭 → 다음 케이스가 전부 통과해야 합니다.

### ✅ 통과해야 하는 케이스 (정상 동작)

**라인업 저장 (관리자)**
- 경로: `/lineup/latest`
- 작업: 쓰기
- 데이터:
  ```json
  { "date": "2026.06.02", "opponent": "두산", "pitcher": "베니지아노", "players": {} }
  ```

**투표 카운트 +1**
- 경로: `/vote/20260602/counts/win`
- 작업: 쓰기
- 데이터: `1`

**투표 유저 기록**
- 경로: `/vote/20260602/users/g_abc1234567890def`
- 작업: 쓰기
- 데이터: `{ "choice": "win", "at": 1717280000000 }`

**응원 톡 메시지**
- 경로: `/chat/20260602/messages/-NXxxx`
- 작업: 쓰기
- 데이터:
  ```json
  { "userId": "g_abc1234567890def", "text": "오늘 SSG 화이팅!", "at": 1717280000000 }
  ```

**페이지뷰 트래킹**
- 경로: `/analytics/daily/2026-06-02/pageviews`
- 작업: 쓰기
- 데이터: `5`

### ❌ 차단되어야 하는 케이스 (악용 시도)

**잘못된 날짜 키**
- 경로: `/vote/abc/counts/win`
- 작업: 쓰기
- 데이터: `1`
- 기대: **거부됨** (date가 8자리 숫자 아님)

**음수 카운트**
- 경로: `/vote/20260602/counts/win`
- 작업: 쓰기
- 데이터: `-1000`
- 기대: **거부됨**

**51자 이상 메시지**
- 경로: `/chat/20260602/messages/-NXyyy`
- 데이터:
  ```json
  { "userId": "g_a", "text": "아주아주아주아주아주아주아주아주아주아주아주아주아주아주아주아주아주아주아주아주아주아주아주아주아주아주", "at": 1717280000000 }
  ```
- 기대: **거부됨**

**기존 메시지 덮어쓰기 (도배 방어)**
- 경로: `/chat/20260602/messages/-NXzzz` (이미 존재)
- 작업: 쓰기
- 기대: **거부됨**

**차단된 유저의 메시지**
- `/chat/banned/g_blockeduser`에 `{ at: 123 }` 있을 때
- 경로: `/chat/20260602/messages/-NXnew`
- 데이터: `{ "userId": "g_blockeduser", "text": "안녕", "at": 1717280000000 }`
- 기대: **거부됨**

**알 수 없는 최상위 경로**
- 경로: `/myCustomTable/test`
- 작업: 쓰기
- 기대: **거부됨** ($other 규칙)

## 솔직한 한계

이 룰이 **막지 못하는 것**:

1. **IP 단위 rate limit** — Firebase Realtime Rules는 불가능. 누군가 분당 100번 요청해도 룰만으로는 못 막음 → 운영자 모니터링 + `chat/banned` 차단으로 대응
2. **신원 인증** — Firebase Anonymous Auth 도입 안 했으므로 같은 유저인지 보장 안 됨. 시크릿 모드로 새 ID 발급 가능
3. **관리자 vs 일반 구분** — `isAdmin`이 클라이언트 state라 룰에서 구분 불가. 누구든 cURL로 `lineup/latest` 덮어쓰기 가능 (데이터 무결성 체크만 통과하면)

## 다음 단계 권장사항

이 룰은 **임시 방어선**입니다. 출시 후 트래픽이 늘면:

1. **Firebase Anonymous Auth 도입** — 클라이언트에 안정적 uid 부여 → 룰에서 `auth.uid` 활용 가능
2. **관리자 write는 Vercel API로 이전** — 클라이언트가 룰을 우회해도 admin 데이터는 안전
3. **App Check 활성화** — 합법 클라이언트(우리 앱)만 데이터베이스 호출 가능. 가장 강력한 방어선

지금은 1~2시간 들여 룰만 적용하고, 트래픽 늘면 위 3가지 차례로 도입하면 됩니다.
